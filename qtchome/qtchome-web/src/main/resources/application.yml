eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8001/eureka/
  instance:
    prefer-ip-address: true
security:
  basic:
    enabled: false
server:
  port: 8006
logging:
  level: debug
spring:
  application:
    name: api-home
ribbon:
   ReadTimeout: 60000
   ConnectTimeout: 30000
